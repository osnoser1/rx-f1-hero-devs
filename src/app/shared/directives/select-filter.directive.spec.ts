import { SelectFilterDirective } from './select-filter.directive';

describe('SelectFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectFilterDirective();
    expect(directive).toBeTruthy();
  });
});
